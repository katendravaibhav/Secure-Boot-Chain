Notes:
- The bootloader verification is a stub; replace with hardware-backed verification.
- The logging daemon uses OpenSSL (libcrypto) for SHA-256 chaining.
- The HSM simulator uses `cryptography` Python package.
