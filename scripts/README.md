Scripts

- `scripts/sign_image.py` : sign an image using a private key (POC)
- `hsm/hsm_sim.py` : simple HSM simulator for signing and verifying images

See `keys/README.md` for generating test keys.
